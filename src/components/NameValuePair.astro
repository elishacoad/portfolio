---
export interface Props {
  name: string;
  value: string;
  noMargin?: boolean;
}

const { name, value, noMargin = false } = Astro.props;
---

<div class={noMargin ? '' : 'mt-5'}>
  <h4>{name}</h4>
  <p>{value}<br></p>
</div>
